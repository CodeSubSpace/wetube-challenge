extends base.pug

block content
    h1 TXT2HTML!
    form(method="POST", enctype="multipart/form-data")
        input(type="file", name="textFile", accept="text/*", required)
        input(type="submit", value="Convert!")